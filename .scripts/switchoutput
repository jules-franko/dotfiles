#/bin/bash
#Change audio sinks

sink=$( pactl list sinks short | awk '{print $2}' | dmenu -nb "#222222" -sb "#303030" -fn terminus -y 300 -x 1039 -z 500 -i -l 10) || exit 0
pactl set-default-sink "$sink"
